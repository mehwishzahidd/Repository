One markdown file per practised design (`url_shortener.md`, `inference_api.md`, ...):
requirements → estimates → API → data model → diagram (ASCII or an image) → deep dive →
bottlenecks → what the "interviewer" asked → what I'd fix next time.
