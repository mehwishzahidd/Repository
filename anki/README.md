# Anki deck
`glossary.tsv` is generated from `GLOSSARY.md` (`make anki`). Import into Anki (free):
File → Import → pick the .tsv → fields separated by Tab → Front = term, Back = meaning.
Review five minutes a day; that's plenty. Cards are for vocabulary only — code is learned
by typing, not by flashcards.
